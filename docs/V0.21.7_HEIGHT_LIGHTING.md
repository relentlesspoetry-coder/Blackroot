# V0.21.7 — Height Lighting and Ambient Occlusion

> "I want the world to feel more 3D without actually being 3D."

A flat-shaded surface reads as flat no matter how much texture sits on it, because **the eye judges
form from light, not from detail.** V0.21.5 gave the ground its texture; this gives it shape.

Nothing here is 3D — it is per-tile shading computed from neighbouring elevations. But it is the same
information a 3D renderer would use, which is why it reads as volume.

## Three effects, all baked into the chunk layer

| effect | what it does |
|---|---|
| **directional light** | fixed north-west sun; ground sloping toward it lightens, away darkens. Slope from the elevation gradient across neighbours, so a hillside gets a consistently lit face and a consistently shaded one instead of uniform colour. |
| **ambient occlusion** | tiles hemmed in by higher ground darken proportional to how much taller the surroundings are — **biased toward the side the high ground sits on**, so it reads as a shadow cast by that mass rather than uniform dimming. |
| **edge highlight** | the lip of a drop catches light. One bright line at the top of a step does more for perceived relief than any amount of shading below it. |

Lighting draws **after** the texture pass, so pebbles and grass sit *inside* the shading rather than
floating on top of it. That ordering is what sells the volume.

## This lands the cliff-foot shadow that four attempts failed to produce

Documented as unfixed in V0.20.94 and carried as an open item ever since.

Every earlier attempt tried to **project geometry** down onto the ground. This reads the **height
field** directly and darkens tiles by how much taller their neighbours are. The occlusion *is* the
contact shadow — approached from the data rather than from the geometry, which is why it works.

## Verified

| check | result |
|---|---|
| occluded tiles below flat ground | **11.7 luma** — without the pass, 8.2. A **43% deepening**. |
| drift between repeated arms | ~0.4 luma — signal:noise ≈ **9:1** |
| boot suite | PASS |
| **in-game FPS** | **52** |

## Performance, answered properly at last

Three earlier readings this session were **all artifacts**:

| reading | reality |
|---|---|
| 114 ms/frame (synthetic loop) | preview pane is `document.hidden`, rAF throttled |
| 0 FPS (HUD in pane) | same cause |
| stub savings 3.00–8.74 ms | inside a 29% drift band |

Measured in the real game window: **52 FPS** with everything active. The terrain work is not a
performance problem, and the panic that nearly reverted it was unfounded.

## Remaining

- cliff *faces* still smooth — the reverted V0.21.6 stonework, to be rebuilt inside `buildTerrainChunk`
- fog still drains saturation to 0.017 when weather turns
- 7.6 luma / 0.027 saturation to the reference

## Files changed

`render/terrain-renderer.js`, `Blackroot V0.21.6.html → Blackroot V0.21.7.html`, `core/config.js`,
`PATCH_NOTES.md`, `VERSION.txt`, `README.txt`, this report.
