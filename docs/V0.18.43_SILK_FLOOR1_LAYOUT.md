# V0.18.43 — Silk Web Caverns Floor 1 rebuilt to the floor-plan reference

You provided a "Silk Web Caverns" floor plan and, when asked how to approach a large untestable structural change, chose **"Floor 1 first, then 2 & 3."** So this ships the **Floor 1** rebuild for playtest; Floors 2 & 3 keep their existing layout until Floor 1 is confirmed.

## Floor 1 layout (`systems/dungeon-system.js` `SILK_WEB_FLOOR_CONFIG[1]`)
Left-to-right flow matching the plan:

| Plan room | Config id | Kind | Notes |
|---|---|---|---|
| Entrance from Dark Woods | `safe_entry` | safe_entry | Lantern-Safe Entry Alcove + safe camp (`rooms[0]`, dungeon exit) |
| ① Webbed Tunnels | `webbed_tunnels` | web_choke | narrow |
| ② Spider Cavern | `spider_cavern` | combat | large; **underground pool**; safe camp |
| ③ Web Bridge Room | `web_bridge_room` | web_choke | |
| ④ Egg Sacs Chamber | `egg_sacs_chamber` | egg_room | |
| (mini-boss) | `brood_approach` | combat | **Threadjaw Alpha** mini-boss |
| (web obstacle) | `gate` | puzzle | **Sealed Web Gate** — only path to the boss; opens when Threadjaw Alpha dies |
| (dead end) | `silk_dead_end` | combat | Collapsed Silk Pocket (leaf off the egg chamber) |
| ⑤ Broodmother Arachnis (BOSS) | `boss` | boss | boss room |
| (stairs down) | `treasure` | treasure | Silk Descent Drop (`rooms[last]`, stairs to Floor 2) |

**Links (connected graph):** `safe_entry → webbed_tunnels → spider_cavern → {web_bridge_room, egg_sacs_chamber}`; `web_bridge_room → brood_approach → gate`; `egg_sacs_chamber → {gate, silk_dead_end}`; `gate → boss → treasure`.

**Reachability** was validated programmatically: no orphaned room references, all 10 rooms reachable from the entrance, and both the boss and the descent are reachable. The boss is only reachable through the gate, which the mini-boss gates — so the intended progression is enforced.

The **narrow tunnels + pocket caverns** (V0.18.41) and the **boss web-seal + attack telegraphs** (V0.18.42) all apply to the new layout.

## Supporting changes (so the plan renders correctly)
- **Pools are now tag-aware** — `carveSilkWebFishingPools` carves the water pool into rooms tagged `underground_pool` (so Floor 1's pool lands in **Spider Cavern**), falling back to the old index-based placement on floors without such a tag (Floors 2 & 3 unchanged).
- **Boss-seal entrance is derived from the links** — `startSilkBossRoomSeal` now finds the boss room's non-treasure link neighbour (the gate) as the sealed doorway, instead of the hardcoded `skirr_approach`, so the seal covers the correct entrance for the rebuilt layout.

## Files changed
- `systems/dungeon-system.js` — Floor 1 `SILK_WEB_FLOOR_CONFIG[1]` rewrite; tag-aware pool carving; link-derived boss-seal approach.
- Version/docs: `core/config.js`, `Blackroot V0.18.42.html` → **`Blackroot V0.18.43.html`** (title + patch-note entry), `PATCH_NOTES.md`, `VERSION.txt`, `README.txt`, this report.

**Backup:** the previous version was snapshotted to `backups/Blackroot V0.18.42.zip` (pristine V0.18.42 restored from the V0.18.42 stash — verified 0 V0.18.43 markers — then the V0.18.43 change restored byte-identically). No backups overwritten.

## Validation
- `node --check` — clean on all changed files and the full non-`blender/` JS set.
- JSON — all non-`blender/` JSON parses clean.
- Runtime (served `Blackroot V0.18.43.html`): boots with **0 console errors**.
- **Floor 1 room-ids + reachability validated programmatically** (all referenced ids defined; every room reachable from the entrance; boss + descent reachable).
- **Why Floor 1 only, and why it needs your eyes:** I can't playtest the generated dungeon in this environment (the account→dungeon flow isn't scriptable and the screenshot tool is wedged). The config is structurally validated, but the *feel* — pacing, room sizes, whether the pool/gate/dead-end land where you want, and that it actually reaches the boss in-game — needs you to walk it. That's exactly why we're doing one floor first.

## Manual tests
1. Re-enter the Silk Web Cavern (Floor 1) → confirm the flow: Entrance → Webbed Tunnels → Spider Cavern (pool) → Web Bridge Room / Egg Sacs Chamber → the gate → Broodmother Arachnis (boss) → stairs down.
2. Confirm the gate blocks the boss until you kill the mini-boss (Threadjaw Alpha) in the Broodwarden Approach, then opens.
3. Confirm the dead-end pocket is a dead end, the pool is in Spider Cavern, and the boss room seals when you engage the boss (V0.18.42).
4. If Floor 1 plays well, tell me and I'll rebuild Floors 2 & 3 to their floor-plan rooms the same way.
